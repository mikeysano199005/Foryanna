Drop an optional background track here as `theme.mp3`.

If this file is absent, the site works exactly the same — the music
button in the top-right simply never appears. No code changes needed
to add it later.
